export class Tab {
  icon: string;
  route: string;

  /**
   * Constructor
   * @param icon
   * @param route
   */
  constructor (icon: string, route: string) {
    this.icon = icon;
    this.route = route;
  }
}
